describe('onPreCommit', () => {
  it.todo('should amend contrib locs')

  it.todo('should count the new percentageOfTotal for every git user')

  it.todo('should skip users that match the skipUsers pattern')
})
